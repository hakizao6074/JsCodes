/**
 * @file array-5.js
 * @description Mostra como alterar um elemento de um array e exibir antes/depois.
 */
const cores = ["vermelho", "azul", "amarelo"];
console.log("array original:", cores);
cores[1] = "verde";
console.log("array modificado", cores);
