/**
 * @file arrayPush-3.js
 * @description Mostra que push() retorna a nova quantidade de elementos do array.
 */
let frutas = ["maça","banana"];
let novaQuant = frutas.push("Manga");

console.log(novaQuant)
console.log(frutas)