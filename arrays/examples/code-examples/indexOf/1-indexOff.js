// Criando um array com números
let numeros = [10,20,30,40,50]

// indexOf(20) retorna o índice do elemento 20 no array (que é 1)
console.log(numeros.indexOf(20))


// indexOf(10) retorna o índice do elemento 10 no array (que é 0)
console.log(numeros.indexOf(10)) // Retornará 0
